---
paths:
  - "**/*.env*"
  - "**/config/**"
  - "**/*secret*"
  - "**/*credential*"
---

- 秘密情報をコードやテストに書かない。`.env.example` にキー名だけを追加する。
- 秘密情報がすでにコミットされているのを見つけたら、**自分で履歴を書き換えない**。
  Issue を `agent:blocked` にして人間に報告する（ローテーションが必要なため）。
