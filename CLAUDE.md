# このリポジトリでの作業ルール

<!-- 200行を超えないこと。手順は .claude/skills/ に、パス限定の制約は .claude/rules/ に置く。 -->

## プロジェクト概要

<!-- TODO: 何のリポジトリか、主要ディレクトリ構成を3〜5行で -->

## 検証コマンド（唯一の正）

```bash
./scripts/verify.sh          # 整形 + lint + 型 + テスト + カバレッジ下限。push 前に必ず緑にする
./scripts/verify.sh --fast   # テストだけ（実装ループ中の高速確認用）
./scripts/verify.sh --fix    # フォーマッタと自動修正を当ててから全部走らせる
```

CI もこのスクリプトを呼びます。**ローカルと CI で違うコマンドを使わないこと。**

- 整形で赤くなったら、手で直さず `--fix` を使うこと。
- **カバレッジに下限がある**ので、分岐を足したらテストも足す必要があります。
  下限を下げて通す変更は禁止です（どうしても必要なら `agent:blocked` にして人間に相談）。

## ブランチとコミット

- 作業ブランチ: `feat/issue-<番号>-<slug>` / `fix/issue-<番号>-<slug>`
- 既定ブランチへの直接コミット・直接 push は禁止（フックが止めます）
- コミットメッセージ: `type(scope): 要約`。本文に「なぜ」を書く。何をしたかは差分を読めば分かる
- 1 Issue = 1 ブランチ = 1 PR

## Issue ラベルによる状態管理

ループの状態は**すべて GitHub Issue のラベルで表現**します。セッションが落ちても、
ラベルを見れば続きから再開できます。

| ラベル | 意味 | 次に起きること |
| --- | --- | --- |
| `agent:ready` | 着手可 | エージェントが拾う |
| `agent:wip` | 作業中（ロック） | 他の Issue に着手してはいけない |
| `agent:review` | PR 作成済み | Phase 1 は人間がマージ / Phase 2+ は自動着地 |
| `agent:blocked` | 人間の判断待ち | 人間が `agent:ready` に戻すまで触らない |
| `agent:done` | 完了 | — |

## スコープの規律

- Issue に書かれていない変更をしない。気づいた別の問題は**直さずに Issue を立てる**
- 依存関係の追加・更新は Issue で明示されているときだけ
- `.github/workflows/` と `.claude/settings.json` は編集しない（権限で拒否されます）

## 完了の定義（Definition of Done）

1. 受け入れ条件がテストで表現され、そのテストが**実装前に落ちることを確認済み**
2. `./scripts/verify.sh` が緑
3. CI が緑
4. `pr-reviewer` の VERDICT が APPROVE、または Non-blocking 指摘のみ
5. PR 本文に `Closes #<Issue番号>`

## 詰まったときの作法

推測で進めず、`agent:blocked` を付けて Issue に「何をしようとして・どこで詰まり・何を試し・
人間に何を判断してほしいか」を書いて止まること。中途半端な完了報告が、無人ループで最も高くつきます。
